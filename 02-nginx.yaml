## Working on the nginx server yaml file

- name: install and start nginx server
  hosts: frontend
  become: yes # to get root privileges
  tasks:
   - name: install nginx server
     ansible.builtin.package:
       name: nginx
       state: present

   - name: start nginx server
     ansible.builtin.service:
        name: nginx
        state: started
        enabled: true